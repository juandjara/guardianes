import React from 'react'

const Loading = () => (
  <div style={{
    fontFamily: 'sans-serif', padding: '2rem', textAlign: 'center'
  }}>
    <p style={{marginBottom: 8, fontSize: '1.4rem'}}>
      <strong>Asociación Guardianes</strong>
    </p>
    <span>Cargando ...</span>
  </div>
)

export default Loading
